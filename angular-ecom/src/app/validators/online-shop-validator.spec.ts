import { OnlineShopValidator } from './online-shop-validator';

describe('OnlineShopValidator', () => {
  it('should create an instance', () => {
    expect(new OnlineShopValidator()).toBeTruthy();
  });
});
